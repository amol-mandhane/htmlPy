"""
:py:mod:`htmlPy.settings` has 3 variables.

Attributes:
    ENABLE: Used for enabling some setting
    DISABLE: Used for enabling some setting
    INPUTS_ONLY: Currently used only to ``DISABLE`` right clicking on
        application except for input fields.

"""

ENABLE = 1
DISABLE = 0
INPUTS_ONLY = 2
